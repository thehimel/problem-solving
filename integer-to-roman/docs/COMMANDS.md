# Commands

```shell
conda create --name roman python=3.7
conda activate roman

pip install autopep8
pip install flake8
pip install pytest

pip install -r requirements.txt
pip freeze > requirements.txt

pytest
pytest -q test_solution.py
```
